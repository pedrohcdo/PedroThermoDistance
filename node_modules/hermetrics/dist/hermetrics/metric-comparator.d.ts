import Metric from './metric';
import ComparatorSimilarity from '../interfaces/comparator-opts.interface';
declare class MetricComparator {
    private readonly metrics;
    constructor(metrics?: Metric[]);
    /**
     * similarity
     */
    similarity(source: string, target: string): ComparatorSimilarity[];
}
export default MetricComparator;
