"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var jaro_1 = __importDefault(require("./jaro"));
var JaroWinkler = /** @class */ (function (_super) {
    __extends(JaroWinkler, _super);
    function JaroWinkler(name) {
        if (name === void 0) { name = 'JaroWinkler'; }
        return _super.call(this, name) || this;
    }
    /**
       * Jaro Winkler Similarity
       * @param source
       * @param target
       * @param cost
       * @param p
       */
    JaroWinkler.prototype.similarity = function (source, target, _a) {
        var _b = _a === void 0 ? {} : _a, insertionCost = _b.insertionCost, deletionCost = _b.deletionCost, substitutionCost = _b.substitutionCost, lambdaCost = _b.lambdaCost, roCost = _b.roCost;
        var p = roCost !== null && roCost !== void 0 ? roCost : 0.1;
        if (!(p >= 0 && p <= 0.25)) {
            throw new Error('The p parameter must be between 0 and 0.25');
        }
        var l = 0;
        var maxL = lambdaCost !== null && lambdaCost !== void 0 ? lambdaCost : 4;
        for (var i = 0; i < maxL; i++) {
            if (source[i] !== target[i])
                break;
            l++;
        }
        var j = _super.prototype.similarity.call(this, source, target, { insertionCost: insertionCost, deletionCost: deletionCost, substitutionCost: substitutionCost });
        return j + l * p * (1 - j);
    };
    /**
       * Jaro Winkler distance
       * @param source
       * @param target
       * @param cost
       * @param p
       */
    JaroWinkler.prototype.distance = function (source, target, _a) {
        var _b = _a === void 0 ? {} : _a, insertionCost = _b.insertionCost, deletionCost = _b.deletionCost, substitutionCost = _b.substitutionCost, lambdaCost = _b.lambdaCost, roCost = _b.roCost;
        return 1 - this.similarity(source, target, { insertionCost: insertionCost, deletionCost: deletionCost, substitutionCost: substitutionCost, lambdaCost: lambdaCost, roCost: roCost });
    };
    return JaroWinkler;
}(jaro_1.default));
exports.default = JaroWinkler;
