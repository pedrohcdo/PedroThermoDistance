import Metric from './metric';
import JaroCostOptions from './../interfaces/jaro-opts.interface';
declare class Jaro extends Metric {
    constructor(name?: string);
    /**
     * Jaro Similarity
     * @param source
     * @param target
     * @param cost
     */
    similarity(source: string, target: string, { deletionCost, insertionCost, substitutionCost }?: JaroCostOptions): number;
    /**
     * Jaro distance
     * @param source
     * @param target
     * @param cost
     */
    distance(source: string, target: string, { deletionCost, insertionCost, substitutionCost }?: JaroCostOptions): number;
}
export default Jaro;
