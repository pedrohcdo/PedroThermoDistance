import Metric from './../hermetrics/metric';
import MetricCostOptions from './../interfaces/metric-opts.interface';
declare class Hamming extends Metric {
    constructor(name?: string);
    distance(source: string, target: string, { cost }?: MetricCostOptions): number;
    maxDistance(source: string, target: string, { cost }?: MetricCostOptions): number;
}
export default Hamming;
