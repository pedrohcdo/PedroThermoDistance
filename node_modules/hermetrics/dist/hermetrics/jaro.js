"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var metric_1 = __importDefault(require("./metric"));
var Jaro = /** @class */ (function (_super) {
    __extends(Jaro, _super);
    function Jaro(name) {
        if (name === void 0) { name = 'Jaro'; }
        return _super.call(this, name) || this;
    }
    /**
     * Jaro Similarity
     * @param source
     * @param target
     * @param cost
     */
    Jaro.prototype.similarity = function (source, target, _a) {
        var _b = _a === void 0 ? {} : _a, deletionCost = _b.deletionCost, insertionCost = _b.insertionCost, substitutionCost = _b.substitutionCost;
        var sourceLength = source.length;
        var targetLength = target.length;
        if (sourceLength === 0 && targetLength === 0) {
            return 1;
        }
        var matchDistance = Math.floor(Math.max(sourceLength, targetLength) / 2) - 1;
        var sourceMatches = new Array(sourceLength);
        var targetMatches = new Array(targetLength);
        var matches = 0;
        var transpositions = 0;
        var start = 0;
        var end = 0;
        for (var i = 0; i < sourceLength; i++) {
            start = Math.max(0, i - matchDistance);
            end = Math.min(i + matchDistance + 1, targetLength);
            for (var j = start; j < end; j++) {
                if (targetMatches[j])
                    continue;
                if (source[i] === target[j]) {
                    sourceMatches[i] = true;
                    targetMatches[j] = true;
                    matches++;
                    break;
                }
            }
        }
        if (matches === 0)
            return 0;
        var k = 0;
        for (var i = 0; i < sourceLength; i++) {
            if (!sourceMatches[i])
                continue;
            while (!targetMatches[k])
                k++;
            if (source[i] !== target[k])
                transpositions++;
            k++;
        }
        return ((matches / sourceLength) + (matches / targetLength) + ((matches - transpositions / 2) / matches)) / 3;
    };
    /**
     * Jaro distance
     * @param source
     * @param target
     * @param cost
     */
    Jaro.prototype.distance = function (source, target, _a) {
        var _b = _a === void 0 ? {} : _a, deletionCost = _b.deletionCost, insertionCost = _b.insertionCost, substitutionCost = _b.substitutionCost;
        return 1 - this.similarity(source, target, { deletionCost: deletionCost, insertionCost: insertionCost, substitutionCost: substitutionCost });
    };
    return Jaro;
}(metric_1.default));
exports.default = Jaro;
